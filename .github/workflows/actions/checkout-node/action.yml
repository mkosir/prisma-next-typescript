name: 'Composite action - node'
description: 'Checks out the repository and does something'
runs:
  using: 'composite'
  steps:
    - name: Checkout 🛎️
      uses: actions/checkout@v3

    - name: Setup Node.js ⚙️
      uses: actions/setup-node@v3
      with:
        node-version: '18.x'
